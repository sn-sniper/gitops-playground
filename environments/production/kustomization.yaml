apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../components/frontend

# We can easily override things for production here without touching the components
namespace: production
